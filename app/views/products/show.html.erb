<p style="color: green"><%= notice %></p>

<%= render @product %>

<div>
  <%= link_to "Edit this product", edit_product_path(@product) %> |
  <%= link_to "Back to products", products_path %><br>
  <br><%= button_to "Buy", checkout_create_path, params: {id: @product.id}, remote: true %><br>
  <br><%= button_to "Destroy this product", @product, method: :delete %>
</div>
